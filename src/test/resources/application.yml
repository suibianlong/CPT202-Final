spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:module3test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:

  sql:
    init:
      mode: always
      encoding: UTF-8

mybatis:
  mapper-locations: classpath:mapper/*.xml

module3:
  upload-dir: uploads-test
  demo-data-enabled: false
